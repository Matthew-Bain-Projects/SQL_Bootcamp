# SELECT DISTINCT

SELECT DISTINCT column FROM table;

Returns the distinct/unique entries in the column. 

Useful when maybe needing how many years in film database
SELECT DISTINCT release_year FROM film;

